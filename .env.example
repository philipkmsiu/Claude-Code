# Crazyrouter (OpenAI-compatible). Used only by the Vite server proxy — never exposed to the browser.
OPENAI_BASE_URL=https://your-crazyrouter-endpoint/v1
OPENAI_API_KEY=your_api_key
OPENAI_CHAT_MODEL=gpt-4o
